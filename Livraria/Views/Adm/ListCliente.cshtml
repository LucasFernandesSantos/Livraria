@model List<Cliente>

<table class="table table-hover table-striped">
    <thead>
        <tr class="table-dark">
            <th>Nome</th>
            <th>CPF</th>
            <th>Senha</th>
            <th>Data de Nascimento</th>
            <th>Email</th>
            <th>Telefone</th>
            <th>CEP</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (Cliente item in Model)
        {
        <tr class="table-success">
            <td>@item.Nome</td>
            <td>@item.Cpf</td>
            <td>@item.Senha</td>
            <td>@item.DataNascimento</td>
            <td>@item.Email</td>
            <td>@item.Telefone</td>
            <td>@item.Endereco.Cep</td>



            <td>
                <a class="btn btn-danger" asp-action="RemoverCliente" asp-route-id="@item.ClienteId">Remover</a>
            </td>
            <td>
                <a class="btn btn-warning" asp-action="EditarCliente" asp-route-id="@item.ClienteId">Alterar</a>

            </td>
        </tr>
        }
    </tbody>
</table>